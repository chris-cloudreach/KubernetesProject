apiVersion: v1
kind: Service
metadata:
  name: {{ include "DeploySimpleMicroservice.fullname" . }}
  labels:
    {{- include "DeploySimpleMicroservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "DeploySimpleMicroservice.selectorLabels" . | nindent 4 }}
