apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "DeploySimpleMicroservice.fullname" . }}-test-connection"
  labels:
    {{- include "DeploySimpleMicroservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "DeploySimpleMicroservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
